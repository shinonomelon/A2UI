{
  "name": "@a2ui/lit-samples",
  "private": true,
  "version": "0.8.1",
  "description": "A2UI Quiz App",
  "workspaces": [
    "shell"
  ],
  "scripts": {
    "serve:agent": "cd ../../agent/adk/quiz_tutor && uv run .",
    "serve:shell": "cd shell && npm run dev",
    "build:renderer": "cd ../../../renderers/lit && npm install && npm run build",
    "start": "npm install && npm run build:renderer && concurrently -k -n \"SHELL,AGENT\" -c \"magenta,yellow\" \"npm run serve:shell\" \"npm run serve:agent\""
  },
  "devDependencies": {
    "concurrently": "9.2.1"
  }
}
